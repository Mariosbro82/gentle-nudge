/**
 * Public-facing base URL for all external links (NFC chips, profile shares, etc.)
 * This ensures links always point to the custom domain, not the preview/staging domain.
 */
export const PUBLIC_BASE_URL = "https://nfcwear.de";
